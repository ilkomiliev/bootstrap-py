# this points to a custom pypi proxy repository
# export PIP_INDEX_URL=https://my.artifactory/api/pypi-mirror/simple

# this points to the search URL
# export PIP_SEARCH_URL=https://my.artifactory/api/pypi-mirror/pypi

# trusted host
# export PIP_TRUSTED_HOST=my.artifactory

# twine repo, if necessary for deployment
# export TWINE_REPO_URL=https://my.artifactory/api/pypi-myproject

# if self signed certs are needed put the file to them here
# export PIP_CERT=